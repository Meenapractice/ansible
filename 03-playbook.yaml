- name: "Installing and running nginx"
  hosts: web
  become: yes
  tasks:
  - name: "Installing nginx"
    ansible.builtin.package:
      name: nginx
      state: installed

  - name: "Running nginx"
    ansible.builtin.package:
      name: nginx
      state: started
      enabled: yes
