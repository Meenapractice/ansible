- name: "Installing and running nginx"
  hosts: local
  become: yes
  tasks:
  - name: "Installing nginx"
    ansible.builtin.package:
      name: nginx
      state: present

  - name: "Running nginx"
    ansible.builtin.package:
      name: nginx
      state: started
      enabled: yes
